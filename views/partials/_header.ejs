<header>
  <nav class="navbar navbar-expand-md navbar-dark bg-success">
    <a class="navbar-brand" href="/urls">TinyApp</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav">
        <a class="nav-item nav-link" href="/urls">My URLs</a>
        <a class="nav-item nav-link" href="/urls/new">Create New URL</a>
      </div>
        <div style="display: flex; position: absolute; right: 1em;">
          <% if (!username) { %>
            <form method="POST" action="/login">
              <input type = "text" name = "username">
              <input type = "text" name = "username" style="margin: 1em">
              <button class="btn btn-outline-dark" action = "submit">Login</button>
            </form>
          <% } else { %>
            <a class="nav-item nav-link">Logged in as: <%= username %></a>
            <form method="POST" action="/logout">
              <button class="btn btn-outline-dark" action = "submit">Logout</button>
            </form>
          <% } %>
          </div>
  </nav>
</header>